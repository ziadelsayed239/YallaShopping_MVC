@model List<Product>

<div class="container my-5 p-4 bg-light shadow-sm rounded">
    <div class="row mb-3 align-items-center">
        <div class="col-8">
            <h2 class="text-dark fw-bold mb-1">Product Management</h2>
            <p class="text-muted small">Manage and organize your Products below</p>
        </div>
        <div class="col-4 text-end">
            <a class="btn btn-success btn-sm px-4 py-2" asp-controller="Product" asp-action="Create_Update">
                <i class="bi bi-plus-circle me-1"></i> Add New Product
            </a>
        </div>
    </div>

    <div class="card border-0 shadow rounded-3">
        <!-- Table -->
        <table id="tblData" class="table table-striped table-hover table-borderless mb-0">
            <thead class="table-primary text-dark text-center">
                <tr>
                    <th scope="col">Product Name</th>
                    <th scope="col">ISBN</th>
                    <th scope="col">Author</th>
                    <th scope="col">ListPrice</th>
                    <th scope="col">Category</th>
                    <th scope="col"></th>
 
                </tr>
            </thead>
            @* <tbody>
                @foreach (var i in Model)
                {
                    <tr>
                        <td class="align-middle ps-4">@i.Title</td>
                        <td class="align-middle text-center">@i.ISBN</td>
                        <td class="align-middle text-center">@i.Author</td>
                        <td class="align-middle text-center">@i.ListPrice</td>
                        <td class="align-middle text-center">@i.Category.Name</td>
                        <td class="text-center">
                            <div class="btn-group" role="group">
                                <a asp-controller="Product" asp-action="Create_Update" asp-route-Id="@i.Id"
                                   class="btn btn-outline-primary btn-sm">
                                    <i class="bi bi-pencil-square"></i> Edit
                                </a>
                                <a asp-controller="Product" asp-action="Delete" asp-route-Id="@i.Id"
                                   class="btn btn-outline-danger btn-sm ms-2">
                                    <i class="bi bi-trash3-fill"></i> Delete
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody> *@
        </table>
@*         <!-- Footer -->
        <div class="card-footer text-muted text-center small py-2">
            Total Products: @ViewBag.TotalProducts
        </div> *@
    </div>
</div>

@section Scripts{
    <script src="~/js/product.js"></script>
}
